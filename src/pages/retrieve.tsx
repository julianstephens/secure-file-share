const Retrieve = () => {
  return (
    // <Layout>
    //   {isLoading ? (
    //     <Loader />
    //   ) : (

    //   )}
    //   {decrypted && <p>{JSON.stringify(decrypted)}</p>}
    // </Layout>
    <h1>salf</h1>
  );
};
export default Retrieve;
